{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.orc_scout": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": ["idle"],
          "transitions": [
            { "moving": "query.ground_speed > 0.1" },
            { "attacking": "variable.attack_time > 0.0" }
          ]
        },
        "moving": {
          "animations": ["walk"],
          "transitions": [
            { "default": "query.ground_speed <= 0.1" },
            { "attacking": "variable.attack_time > 0.0" }
          ]
        },
        "attacking": {
          "animations": ["attack"],
          "transitions": [
            { "default": "variable.attack_time <= 0.0 && query.ground_speed <= 0.1" },
            { "moving": "variable.attack_time <= 0.0 && query.ground_speed > 0.1" }
          ]
        }
      }
    }
  }
}