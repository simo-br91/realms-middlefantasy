{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.high_elf_warrior": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": ["idle"],
          "transitions": [
            { "moving": "query.modified_move_speed > 0.1" },
            { "attacking": "variable.attack_time > 0" }
          ]
        },
        "moving": {
          "animations": ["walk"],
          "transitions": [
            { "default": "query.modified_move_speed <= 0.1" },
            { "attacking": "variable.attack_time > 0" }
          ]
        },
        "attacking": {
          "animations": ["attack"],
          "transitions": [
            { "default": "query.any_animation_finished" }
          ]
        }
      }
    }
  }
}