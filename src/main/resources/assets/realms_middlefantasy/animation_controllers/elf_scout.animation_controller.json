{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.silvan_elf_scout": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": ["idle"],
          "transitions": [
            { "walking": "query.is_moving" },
            { "attacking": "query.is_delayed_attacking" },
            { "hurt": "query.health < query.previous_health" }
          ]
        },
        "walking": {
          "animations": ["walk"],
          "transitions": [
            { "default": "!query.is_moving" },
            { "attacking": "query.is_delayed_attacking" },
            { "hurt": "query.health < query.previous_health" }
          ]
        },
        "attacking": {
          "animations": ["attack"],
          "transitions": [
            { "default": "query.all_animations_finished" }
          ]
        },
        "hurt": {
          "animations": ["hurt"],
          "transitions": [
            { "default": "query.all_animations_finished" }
          ]
        }
      }
    }
  }
}