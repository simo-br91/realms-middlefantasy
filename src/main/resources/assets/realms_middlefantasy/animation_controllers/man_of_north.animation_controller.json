{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.man_of_north.general": {
      "initial_state": "default",
      "states": {
        "default": {
          "animations": ["idle"],
          "transitions": [
            { "moving": "query.is_moving" },
            { "attacking": "query.is_delayed_attacking" }
          ]
        },
        "moving": {
          "animations": ["walk"],
          "transitions": [
            { "default": "!query.is_moving" },
            { "attacking": "query.is_delayed_attacking" }
          ]
        },
        "attacking": {
          "animations": ["attack"],
          "transitions": [
            { "default": "query.all_animations_finished" }
          ]
        }
      }
    }
  }
}